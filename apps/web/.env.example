# Browser calls use this API origin directly. Local dev defaults to Fastify on port 3001.
NEXT_PUBLIC_API_URL=http://localhost:3001

# Leave empty when the API serves routes at the origin root (/health, /auth, …).
# Set only if your API is intentionally behind a path prefix.
NEXT_PUBLIC_API_PATH_PREFIX=
