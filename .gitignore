node_modules
dist
.next
*.log
.DS_Store
coverage
.turbo
uploads
.cursor/
.vercel

# Environment files — never commit real secrets. Keep example templates only.
.env
.env.*
.env.local
.env.*.local
.env*.local
!.env.example
!.env.*.example

# Deploy-time symlink created on the VM at /opt/plethora (kept out of the repo)
/docker-compose.override.yml
